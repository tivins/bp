import {Icon} from "../src/components/Icon";

document.addEventListener('DOMContentLoaded', () => {

    // document.body.innerHTML = `<tivins-icon icon="check" class="accent p-2 text-xl"></tivins-icon>`

});